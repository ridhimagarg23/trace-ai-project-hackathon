# ------------------------------------------------------------------
# Core web backend
# ------------------------------------------------------------------
fastapi==0.139.0
uvicorn==0.49.0
python-multipart==0.0.32

# ------------------------------------------------------------------
# LLM + configuration
# ------------------------------------------------------------------
openai==2.44.0
python-dotenv==1.2.2
pydantic==2.13.4

# ------------------------------------------------------------------
# SCAMNET integrations
# (direct HTTP client for the Telegram Bot API; also a transitive
# dependency of openai - pinned explicitly since we now import it)
# ------------------------------------------------------------------
httpx==0.28.1

# Google OAuth 2.0 credentials (service account / authorized user) used
# by the Google Drive, Google Sheets and Gmail integrations. Only this
# package is needed: REST calls go through httpx, so the heavy
# google-api-python-client / google-auth-oauthlib packages are NOT
# required to run the server.
google-auth==2.42.0

# ------------------------------------------------------------------
# Frontend build / misc tooling pulled in by earlier experimentation
# (kept only if actually used; not required by backend/api.py)
# ------------------------------------------------------------------
Flask==3.1.3
streamlit==1.58.0
